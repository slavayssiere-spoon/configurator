syntax = "proto3";

package configurator;

option go_package = "github.com/slavayssiere-spoon/configurator";

message Conf {
	string email = 1;
	string message = 2;
}

service configurator {
	rpc Get (Conf) returns (Conf) {}
}
